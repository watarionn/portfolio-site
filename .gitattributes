# HoloScope release bundles are byte-addressed by their manifests.
# Preserve generated bytes exactly and exclude immutable bundle bytes from whitespace normalization checks.
services/holoscope/release/releases/** -text -whitespace
